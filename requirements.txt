# The library and the CLI need nothing but the Python standard library.
# These are only for the optional extras:

flask>=3.0        # the small web front end (python -m csync web)
pytest>=8.0       # the tests
