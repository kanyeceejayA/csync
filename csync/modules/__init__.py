"""Domain modules built on the dictionary-agnostic core.

Each module here gives one dictionary's items their meaning.  `assignments` is
the worked example - copy it and edit its `Fields` block for another survey.
"""
